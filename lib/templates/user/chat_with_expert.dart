import 'package:flutter/material.dart';

class chat_with_expert extends StatelessWidget {
  const chat_with_expert({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('CHAT_WITH_EXPERT'),
      ),
    );
  }
}
