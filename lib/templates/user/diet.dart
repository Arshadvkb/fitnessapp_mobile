import 'package:flutter/material.dart';

class DietTracking extends StatelessWidget {
  const DietTracking({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Track your diet details here.'),
    );
  }
}
