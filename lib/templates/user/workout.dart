import 'package:flutter/material.dart';

class WorkoutTracking extends StatelessWidget {
  const WorkoutTracking({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Track your workout details here.'),
    );
  }
}
